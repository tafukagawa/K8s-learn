# フラッシュカード テストモード Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 既存のフリップカード（FlashCard/FlashcardView）を廃止し、テスト用紙スタイル＋AI採点に全面刷新する

**Architecture:** 全問一覧表示 → 回答入力 → Ollama 経由 AI 採点 → 結果表示。`TestView.tsx` が `FlashcardView.tsx` を置き換え、`TestResultView.tsx` が採点結果を表示する。AI 採点は既存の `electron/ipc/ai.ts` に `gradeAnswers` ハンドラーを追加する。

**Tech Stack:** React/MUI, TypeScript, Electron IPC, Ollama (qwen2.5:3b)

---

### Task 1: 型定義と IpcApi の更新

**Files:**
- Modify: `src/types/index.ts`

- [ ] **Step 1: `src/types/index.ts` に GradeRequest・GradeResult 型と IpcApi 更新を追加**

`IpcApi` インターフェースの直前に追記：

```ts
export interface GradeRequest {
  type: 'command' | 'knowledge'
  id: number
  question: string
  userAnswer: string
  correctAnswer: string
}

export interface GradeResult {
  id: number
  verdict: 'correct' | 'partial' | 'incorrect'
  comment: string
  correctAnswer: string
}
```

`IpcApi` の `ai` フィールドに `gradeAnswers` を追加：

```ts
  ai: {
    checkOllama: () => Promise<{ ok: boolean; models: string[] }>
    generateCloze: (knowledgeId: number) => Promise<ClozeItem[]>
    gradeAnswers: (requests: GradeRequest[]) => Promise<GradeResult[]>
  }
```

- [ ] **Step 2: ビルド確認**

```bash
cd "C:\Users\fukagawa\work\k8s-learning-app" && npm run build:web 2>&1 | tail -20
```

Expected: `gradeAnswers` が未実装のため preload で型エラーが出る。次のタスクで解消する。

- [ ] **Step 3: コミット**

```bash
git add src/types/index.ts
git commit -m "feat: add GradeRequest/GradeResult types and gradeAnswers to IpcApi"
```

---

### Task 2: AI IPC ハンドラーに gradeAnswers を追加

**Files:**
- Modify: `electron/ipc/ai.ts`

- [ ] **Step 1: `electron/ipc/ai.ts` の `createAiHandlers` に `gradeAnswers` を追加**

`generateCloze` メソッドの後に追記：

```ts
    async gradeAnswers(requests: import('../../src/types').GradeRequest[]): Promise<import('../../src/types').GradeResult[]> {
      if (requests.length === 0) return []

      const questionsText = requests.map((r, i) =>
        `[${i + 1}] ID:${r.id}\n問題: ${r.question}\n正解: ${r.correctAnswer.substring(0, 300)}\n回答: ${r.userAnswer || '（未回答）'}`
      ).join('\n\n')

      const prompt = `あなたは日本語の採点者です。以下の各問題について回答を採点し、JSON配列のみを返してください。

採点基準:
- "correct": 概念を正確に説明できている
- "partial": 部分的に正しいが不完全または不正確な部分がある
- "incorrect": 間違っているか未回答

必ずJSON配列のみを返してください（説明文・Markdownコードブロック不要）:
[{"id": <数値>, "verdict": "correct"|"partial"|"incorrect", "comment": "<採点コメント30文字以内の日本語>"}]

問題:
${questionsText}`

      const body = JSON.stringify({
        model: DEFAULT_MODEL,
        messages: [{ role: 'user', content: prompt }],
        stream: false,
      })
      const raw = await ollamaRequest('/api/chat', body)
      const parsed = JSON.parse(raw)
      const content: string = parsed.message?.content ?? ''

      const jsonMatch = content.match(/\[[\s\S]*\]/)
      if (!jsonMatch) throw new Error('採点結果のJSONが取得できませんでした')

      const gradeData: Array<{ id: number; verdict: string; comment: string }> = JSON.parse(jsonMatch[0])

      for (const item of gradeData) {
        const req = requests.find(r => r.id === item.id)
        if (!req) continue
        const status = item.verdict === 'correct' ? 'done' : 'learning'
        db.prepare(`
          INSERT INTO progress (item_type, item_id, status, attempt_count, last_reviewed)
          VALUES (?, ?, ?, 1, datetime('now'))
          ON CONFLICT(item_type, item_id) DO UPDATE SET
            status = excluded.status,
            attempt_count = attempt_count + 1,
            correct_count = correct_count + CASE WHEN excluded.status = 'done' THEN 1 ELSE 0 END,
            last_reviewed = excluded.last_reviewed
        `).run(req.type, req.id, status)
      }

      return gradeData.map(g => ({
        id: g.id,
        verdict: g.verdict as 'correct' | 'partial' | 'incorrect',
        comment: g.comment,
        correctAnswer: requests.find(r => r.id === g.id)?.correctAnswer ?? '',
      }))
    },
```

- [ ] **Step 2: コミット**

```bash
git add electron/ipc/ai.ts
git commit -m "feat: add gradeAnswers IPC handler to ai.ts"
```

---

### Task 3: preload.ts に gradeAnswers を公開

**Files:**
- Modify: `electron/preload.ts`

- [ ] **Step 1: `electron/preload.ts` の `ai` ブロックに `gradeAnswers` を追加**

```ts
  ai: {
    checkOllama: () => ipcRenderer.invoke('ai:checkOllama'),
    generateCloze: (knowledgeId: number) => ipcRenderer.invoke('ai:generateCloze', knowledgeId),
    gradeAnswers: (requests) => ipcRenderer.invoke('ai:gradeAnswers', requests),
  },
```

- [ ] **Step 2: `electron/main.ts` で `ai:gradeAnswers` ハンドラーを登録していることを確認**

`electron/main.ts` を確認し、他の `ai:*` ハンドラーと同様に `ai:gradeAnswers` が登録されているか確認する。登録されていない場合は以下を追加：

```ts
ipcMain.handle('ai:gradeAnswers', (_, requests) => aiHandlers.gradeAnswers(requests))
```

- [ ] **Step 3: ビルド確認**

```bash
cd "C:\Users\fukagawa\work\k8s-learning-app" && npm run build:web 2>&1 | tail -20
```

Expected: エラーなし（TypeScript の型が揃った）

- [ ] **Step 4: コミット**

```bash
git add electron/preload.ts electron/main.ts
git commit -m "feat: expose ai.gradeAnswers in preload"
```

---

### Task 4: TestView.tsx を新規作成

**Files:**
- Create: `src/features/flashcard/TestView.tsx`

- [ ] **Step 1: `src/features/flashcard/TestView.tsx` を作成**

```tsx
import { useState, useEffect, useMemo } from 'react'
import {
  Box, Typography, TextField, Button, ToggleButton, ToggleButtonGroup,
  CircularProgress, Alert,
} from '@mui/material'
import ArrowBackIcon from '@mui/icons-material/ArrowBack'
import { api } from '../../shared/ipc'
import type { CommandWithProgress, KnowledgeWithProgress, GradeRequest, GradeResult } from '../../types'
import { TestResultView } from './TestResultView'

type FilterMode = 'all' | 'unseen' | 'learning'
type FlashItem = CommandWithProgress | KnowledgeWithProgress

function isCommand(item: FlashItem): item is CommandWithProgress {
  return 'name' in item
}

interface TestViewProps {
  categoryId: number
  initialFilter?: FilterMode
  initialSection?: 'commands' | 'knowledge' | 'all'
  onBack?: () => void
}

export function TestView({ categoryId, initialFilter, initialSection, onBack }: TestViewProps) {
  const [commands, setCommands] = useState<CommandWithProgress[]>([])
  const [knowledge, setKnowledge] = useState<KnowledgeWithProgress[]>([])
  const [filter, setFilter] = useState<FilterMode>(initialFilter ?? 'all')
  const [textAnswers, setTextAnswers] = useState<Record<number, string>>({})
  const [clozeAnswers, setClozeAnswers] = useState<Record<number, string[]>>({})
  const [grading, setGrading] = useState(false)
  const [results, setResults] = useState<GradeResult[] | null>(null)
  const [ollamaOk, setOllamaOk] = useState(false)
  const [ollamaChecked, setOllamaChecked] = useState(false)

  useEffect(() => {
    Promise.all([
      api.commands.list(categoryId),
      api.knowledge.list(categoryId),
      api.ai.checkOllama(),
    ]).then(([cmds, kn, ollama]) => {
      setCommands(cmds)
      setKnowledge(kn)
      setOllamaOk(ollama.ok)
      setOllamaChecked(true)
    })
  }, [categoryId])

  const deck = useMemo<FlashItem[]>(() => {
    let base: FlashItem[]
    if (initialSection === 'commands') base = [...commands]
    else if (initialSection === 'knowledge') base = [...knowledge]
    else base = [...commands, ...knowledge]
    if (filter === 'unseen') return base.filter(i => !i.progress || i.progress.status === 'unseen')
    if (filter === 'learning') return base.filter(i => i.progress?.status === 'learning')
    return base
  }, [commands, knowledge, filter, initialSection])

  async function handleGrade() {
    const requests: GradeRequest[] = deck.map(item => {
      if (isCommand(item)) {
        return {
          type: 'command' as const,
          id: item.id,
          question: `「${item.name}」コマンドの説明と用途`,
          userAnswer: textAnswers[item.id] ?? '',
          correctAnswer: item.description + (item.syntax ? `\n構文: ${item.syntax}` : ''),
        }
      }
      const k = item as KnowledgeWithProgress
      if (k.cloze && k.cloze.length > 0) {
        const answers = clozeAnswers[k.id] ?? []
        return {
          type: 'knowledge' as const,
          id: k.id,
          question: k.cloze.map(c => c.q).join(' / '),
          userAnswer: k.cloze.map((c, i) => `${c.q} → ${answers[i] ?? ''}`).join('\n'),
          correctAnswer: k.cloze.map(c => `${c.q} → ${c.a}`).join('\n'),
        }
      }
      return {
        type: 'knowledge' as const,
        id: k.id,
        question: `「${k.title}」について`,
        userAnswer: textAnswers[k.id] ?? '',
        correctAnswer: k.body.substring(0, 600),
      }
    })

    setGrading(true)
    try {
      const gradeResults = await api.ai.gradeAnswers(requests)
      setResults(gradeResults)
    } finally {
      setGrading(false)
    }
  }

  function handleRetry() {
    setResults(null)
    setTextAnswers({})
    setClozeAnswers({})
  }

  if (results) {
    return <TestResultView deck={deck} results={results} onRetry={handleRetry} />
  }

  if (deck.length === 0) {
    return (
      <Box sx={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', height: '60vh', gap: 2 }}>
        <Typography variant="h6" color="text.secondary">このフィルターに該当する問題がありません</Typography>
        <Button onClick={() => setFilter('all')}>全件を表示</Button>
      </Box>
    )
  }

  return (
    <Box sx={{ p: 3, maxWidth: 720, mx: 'auto' }}>
      {onBack && (
        <Button startIcon={<ArrowBackIcon />} size="small" onClick={onBack} sx={{ mb: 2, color: 'text.secondary' }}>
          リファレンスに戻る
        </Button>
      )}

      <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 3 }}>
        <ToggleButtonGroup value={filter} exclusive onChange={(_, v) => v && setFilter(v)} size="small">
          <ToggleButton value="all">全件</ToggleButton>
          <ToggleButton value="unseen">未学習</ToggleButton>
          <ToggleButton value="learning">学習中</ToggleButton>
        </ToggleButtonGroup>
        <Typography variant="body2" color="text.secondary">{deck.length} 問</Typography>
      </Box>

      <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
        {deck.map((item, idx) => {
          if (isCommand(item)) {
            return (
              <Box key={item.id} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2 }}>
                <Typography variant="caption" color="text.secondary">Q{idx + 1} · コマンド</Typography>
                <Box sx={{ fontFamily: 'monospace', fontSize: 14, color: 'primary.main', fontWeight: 700, bgcolor: 'action.hover', p: 1, borderRadius: 1, mt: 0.5, mb: 1 }}>
                  {item.name}
                </Box>
                <Typography variant="caption" color="text.secondary" display="block" sx={{ mb: 0.5 }}>
                  このコマンドの説明と用途を記述してください
                </Typography>
                <TextField
                  multiline rows={2} fullWidth size="small"
                  value={textAnswers[item.id] ?? ''}
                  onChange={e => setTextAnswers(a => ({ ...a, [item.id]: e.target.value }))}
                  placeholder="説明を入力..."
                />
              </Box>
            )
          }
          const k = item as KnowledgeWithProgress
          if (k.cloze && k.cloze.length > 0) {
            return (
              <Box key={k.id} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2 }}>
                <Typography variant="caption" color="text.secondary">Q{idx + 1} · 穴埋め</Typography>
                <Box sx={{ mt: 1 }}>
                  {k.cloze.map((ci, ci_i) => {
                    const parts = ci.q.split('（）')
                    return (
                      <Box key={ci_i} sx={{ display: 'flex', alignItems: 'center', flexWrap: 'wrap', gap: 0.5, mb: 0.5 }}>
                        <Typography component="span" sx={{ fontSize: 14 }}>{parts[0]}</Typography>
                        <Box
                          component="input"
                          value={(clozeAnswers[k.id] ?? [])[ci_i] ?? ''}
                          onChange={e => {
                            const val = e.target.value
                            setClozeAnswers(a => {
                              const arr = [...(a[k.id] ?? k.cloze!.map(() => ''))]
                              arr[ci_i] = val
                              return { ...a, [k.id]: arr }
                            })
                          }}
                          sx={theme => ({
                            border: '1px solid', borderColor: 'primary.main', borderRadius: 0.75,
                            px: 1, py: 0.25, fontSize: 14, minWidth: 80, maxWidth: 160,
                            bgcolor: theme.palette.mode === 'dark' ? '#1e2030' : '#f0f4ff',
                            color: 'text.primary', outline: 'none',
                            '&:focus': { borderColor: 'primary.light', boxShadow: '0 0 0 2px rgba(99,140,255,0.2)' },
                          })}
                        />
                        {parts[1] && <Typography component="span" sx={{ fontSize: 14 }}>{parts[1]}</Typography>}
                      </Box>
                    )
                  })}
                </Box>
              </Box>
            )
          }
          return (
            <Box key={k.id} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2 }}>
              <Typography variant="caption" color="text.secondary">Q{idx + 1} · ナレッジ</Typography>
              <Typography variant="subtitle2" sx={{ mt: 0.5, mb: 1, fontWeight: 700 }}>{k.title}</Typography>
              <Typography variant="caption" color="text.secondary" display="block" sx={{ mb: 0.5 }}>
                この概念を説明してください
              </Typography>
              <TextField
                multiline rows={3} fullWidth size="small"
                value={textAnswers[k.id] ?? ''}
                onChange={e => setTextAnswers(a => ({ ...a, [k.id]: e.target.value }))}
                placeholder="説明を入力..."
              />
            </Box>
          )
        })}
      </Box>

      <Box sx={{ mt: 3 }}>
        {ollamaChecked && !ollamaOk && (
          <Alert severity="warning" sx={{ mb: 2 }}>
            Ollama が起動していません。AI採点を使用するには Ollama を起動してください。
          </Alert>
        )}
        <Button
          variant="contained"
          size="large"
          fullWidth
          onClick={handleGrade}
          disabled={!ollamaOk || grading}
          startIcon={grading ? <CircularProgress size={16} color="inherit" /> : undefined}
        >
          {grading ? '採点中...' : 'AIで採点する'}
        </Button>
      </Box>
    </Box>
  )
}
```

- [ ] **Step 2: ビルド確認**

```bash
cd "C:\Users\fukagawa\work\k8s-learning-app" && npm run build:web 2>&1 | tail -20
```

Expected: エラーなし（TestResultView がまだ未作成の場合はインポートエラーが出るので次の Task で解消）

- [ ] **Step 3: コミット**

```bash
git add src/features/flashcard/TestView.tsx
git commit -m "feat: create TestView component (test paper style)"
```

---

### Task 5: TestResultView.tsx を新規作成

**Files:**
- Create: `src/features/flashcard/TestResultView.tsx`

- [ ] **Step 1: `src/features/flashcard/TestResultView.tsx` を作成**

```tsx
import { Box, Typography, Button, Chip } from '@mui/material'
import CheckCircleIcon from '@mui/icons-material/CheckCircle'
import ErrorIcon from '@mui/icons-material/Error'
import RemoveCircleIcon from '@mui/icons-material/RemoveCircle'
import RefreshIcon from '@mui/icons-material/Refresh'
import type { CommandWithProgress, KnowledgeWithProgress, GradeResult } from '../../types'

type FlashItem = CommandWithProgress | KnowledgeWithProgress

function isCommand(item: FlashItem): item is CommandWithProgress {
  return 'name' in item
}

interface TestResultViewProps {
  deck: FlashItem[]
  results: GradeResult[]
  onRetry: () => void
}

export function TestResultView({ deck, results, onRetry }: TestResultViewProps) {
  const correctCount = results.filter(r => r.verdict === 'correct').length
  const partialCount = results.filter(r => r.verdict === 'partial').length
  const incorrectCount = results.filter(r => r.verdict === 'incorrect').length
  const score = correctCount + partialCount * 0.5

  const resultMap = new Map(results.map(r => [r.id, r]))

  return (
    <Box sx={{ p: 3, maxWidth: 720, mx: 'auto' }}>
      {/* スコアサマリー */}
      <Box sx={{ textAlign: 'center', mb: 4, p: 3, border: '1px solid', borderColor: 'divider', borderRadius: 2 }}>
        <Typography variant="caption" color="text.secondary" display="block" sx={{ mb: 0.5 }}>
          AI 採点結果
        </Typography>
        <Typography variant="h3" sx={{ fontWeight: 800, my: 1 }}>
          {score}
          <Typography component="span" variant="h5" color="text.secondary"> / {deck.length}</Typography>
        </Typography>
        <Box sx={{ display: 'flex', justifyContent: 'center', gap: 1 }}>
          <Chip label={`正解 ${correctCount}`} color="success" size="small" variant="outlined" />
          <Chip label={`部分点 ${partialCount}`} color="warning" size="small" variant="outlined" />
          <Chip label={`不正解 ${incorrectCount}`} color="error" size="small" variant="outlined" />
        </Box>
      </Box>

      {/* 問題別結果 */}
      <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2, mb: 3 }}>
        {deck.map((item, idx) => {
          const result = resultMap.get(item.id)
          if (!result) return null
          const label = isCommand(item) ? item.name : (item as KnowledgeWithProgress).title
          const color = result.verdict === 'correct' ? 'success' : result.verdict === 'partial' ? 'warning' : 'error'
          const Icon = result.verdict === 'correct'
            ? CheckCircleIcon
            : result.verdict === 'partial'
              ? RemoveCircleIcon
              : ErrorIcon

          return (
            <Box
              key={item.id}
              sx={{
                p: 2,
                border: '1px solid',
                borderColor: `${color}.main`,
                borderLeft: '4px solid',
                borderLeftColor: `${color}.main`,
                borderRadius: 2,
              }}
            >
              <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 1 }}>
                <Typography variant="caption" color="text.secondary">Q{idx + 1}</Typography>
                <Chip
                  icon={<Icon fontSize="small" />}
                  label={result.verdict === 'correct' ? '正解' : result.verdict === 'partial' ? '部分点' : '不正解'}
                  color={color}
                  size="small"
                />
              </Box>
              <Typography
                variant="subtitle2"
                sx={{ fontWeight: 700, mb: 0.5, fontFamily: isCommand(item) ? 'monospace' : 'inherit' }}
              >
                {label}
              </Typography>
              <Typography variant="caption" color="text.secondary" display="block">
                AI コメント: {result.comment}
              </Typography>
              {result.verdict !== 'correct' && (
                <Box sx={{ mt: 1, p: 1, bgcolor: 'action.hover', borderRadius: 1 }}>
                  <Typography variant="caption" color="text.secondary">正解: </Typography>
                  <Typography variant="caption">{result.correctAnswer.substring(0, 200)}</Typography>
                </Box>
              )}
            </Box>
          )
        })}
      </Box>

      <Button variant="contained" fullWidth startIcon={<RefreshIcon />} onClick={onRetry}>
        もう一度
      </Button>
    </Box>
  )
}
```

- [ ] **Step 2: ビルド確認**

```bash
cd "C:\Users\fukagawa\work\k8s-learning-app" && npm run build:web 2>&1 | tail -20
```

Expected: エラーなし

- [ ] **Step 3: コミット**

```bash
git add src/features/flashcard/TestResultView.tsx
git commit -m "feat: create TestResultView component"
```

---

### Task 6: App.tsx を更新し旧ファイルを削除

**Files:**
- Modify: `src/App.tsx`
- Delete: `src/features/flashcard/FlashcardView.tsx`
- Delete: `src/features/flashcard/FlashCard.tsx`

- [ ] **Step 1: `src/App.tsx` の import を更新**

```ts
// 変更前
import { FlashcardView } from './features/flashcard/FlashcardView'

// 変更後
import { TestView } from './features/flashcard/TestView'
```

- [ ] **Step 2: `src/App.tsx` の JSX を更新**

```tsx
// 変更前
        {view.mode === 'flashcard' && flashcardCategoryId != null && (
          <FlashcardView
            categoryId={flashcardCategoryId}
            initialSection={flashcardConfig.section}
            initialFilter={flashcardConfig.filter}
            onBack={() => {
              if (lastRefView.current) setView(lastRefView.current)
            }}
          />
        )}

// 変更後
        {view.mode === 'flashcard' && flashcardCategoryId != null && (
          <TestView
            categoryId={flashcardCategoryId}
            initialSection={flashcardConfig.section}
            initialFilter={flashcardConfig.filter}
            onBack={() => {
              if (lastRefView.current) setView(lastRefView.current)
            }}
          />
        )}
```

- [ ] **Step 3: 旧ファイルを削除**

```bash
rm "C:\Users\fukagawa\work\k8s-learning-app\src\features\flashcard\FlashcardView.tsx"
rm "C:\Users\fukagawa\work\k8s-learning-app\src\features\flashcard\FlashCard.tsx"
```

または PowerShell で：

```powershell
Remove-Item "src\features\flashcard\FlashcardView.tsx"
Remove-Item "src\features\flashcard\FlashCard.tsx"
```

- [ ] **Step 4: ビルド確認**

```bash
cd "C:\Users\fukagawa\work\k8s-learning-app" && npm run build:web 2>&1 | tail -20
```

Expected: エラーなし。`FlashCard` や `FlashcardView` への参照がすべて解決されていることを確認する。

- [ ] **Step 5: 動作確認**

1. アプリを起動する
2. サイドバーでフラッシュカードモードに切り替える
3. テスト用紙形式で全問が表示されることを確認
4. いくつかの問題に回答を入力する
5. 「AIで採点する」ボタンをクリック（Ollamaが起動している場合）
6. 採点結果が表示され、正解/部分点/不正解が表示されることを確認
7. 「もう一度」ボタンで回答フォームにリセットされることを確認
8. Ollamaが起動していない場合、警告メッセージが表示されボタンが無効になることを確認

- [ ] **Step 6: 最終コミット**

```bash
git add src/App.tsx
git add -u src/features/flashcard/
git commit -m "feat: replace FlashCard with TestView (test paper + AI grading)"
```
